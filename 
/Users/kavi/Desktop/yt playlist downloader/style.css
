
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', -apple-system, BlinkMacSystemFont, sans-serif;
    background: linear-gradient(135deg, #87CEEB 0%, #B0E0E6 50%, #AFEEEE 100%);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #2c3e50;
    padding: 1rem;
}

.container {
    background: #f8fbff;
    padding: 2.5rem;
    border-radius: 25px;
    box-shadow: 
        12px 12px 24px rgba(135, 206, 235, 0.4),
        -12px -12px 24px rgba(255, 255, 255, 0.9),
        inset 6px 6px 12px rgba(255, 255, 255, 0.6),
        inset -6px -6px 12px rgba(135, 206, 235, 0.25);
    max-width: 600px;
    width: 95%;
    text-align: center;
    border: 2px solid rgba(135, 206, 235, 0.4);
    backdrop-filter: blur(10px);
}

h1 {
    margin-bottom: 2rem;
    color: #1e3a8a;
    font-size: 2.2rem;
    font-weight: 700;
    text-shadow: 2px 2px 4px rgba(255,255,255,0.8);
}

.input-group {
    display: flex;
    gap: 1rem;
    margin-bottom: 2rem;
    flex-wrap: wrap;
}

#playlistUrl {
    flex: 1;
    padding: 1.2rem 1.5rem;
    border: none;
    border-radius: 15px;
    font-size: 1.1rem;
    background: rgba(255,255,255,0.8);
    box-shadow: 
        inset 4px 4px 8px rgba(135, 206, 235, 0.3),
        inset -3px -3px 6px rgba(255, 255, 255, 0.9);
    min-width: 300px;
}

#playlistUrl:focus {
    outline: none;
    box-shadow: 
        inset 2px 2px 4px rgba(135, 206, 235, 0.4),
        0 0 20px rgba(135, 206, 235, 0.5);
}

button {
    padding: 1.2rem 2.5rem;
    background: linear-gradient(135deg, #87CEEB 0%, #5F9EA0 100%);
    color: #1e3a8a;
    border: none;
    border-radius: 15px;
    cursor: pointer;
    font-size: 1.1rem;
    font-weight: 600;
    transition: all 0.3s ease;
    box-shadow: 
        8px 8px 16px rgba(135, 206, 235, 0.5),
        -8px -8px 16px rgba(255, 255, 255, 0.95),
        inset 4px 4px 8px rgba(255, 255, 255, 0.7);
}

button:hover:not(:disabled) {
    transform: translateY(-3px);
    box-shadow: 
        6px 6px 12px rgba(135, 206, 235, 0.6),
        -6px -6px 12px rgba(255, 255, 255, 1),
        inset 3px 3px 6px rgba(255, 255, 255, 0.8);
}

button:active {
    transform: translateY(-1px);
}

button:disabled {
    opacity: 0.6;
    cursor: not-allowed;
    box-shadow: none;
    transform: none;
}

#formatGroup {
    display: flex;
    gap: 1rem;
    margin-bottom: 2rem;
    justify-content: center;
    flex-wrap: wrap;
}

select {
    padding: 1.2rem 1.5rem;
    border: none;
    border-radius: 15px;
    font-size: 1.1rem;
    background: rgba(255,255,255,0.8);
    box-shadow: 
        inset 4px 4px 8px rgba(135, 206, 235, 0.3),
        inset -3px -3px 6px rgba(255, 255, 255, 0.9);
    cursor: pointer;
    min-width: 140px;
}

select:focus {
    outline: none;
    box-shadow: 
        inset 2px 2px 4px rgba(135, 206, 235, 0.4),
        0 0 15px rgba(135, 206, 235, 0.6);
}

.hidden {
    display: none !important;
}

.progress {
    margin-top: 1.5rem;
    padding: 1.5rem;
    background: rgba(255,255,255,0.6);
    border-radius: 20px;
    box-shadow: inset 4px 4px 8px rgba(0,0,0,0.1);
}

.progress-bar {
    width: 100%;
    height: 14px;
    background: rgba(135, 206, 235, 0.4);
    border-radius: 7px;
    overflow: hidden;
    margin-bottom: 1.5rem;
    box-shadow: inset 3px 3px 6px rgba(0,0,0,0.1);
}

.progress-bar > div {
    height: 100%;
    background: linear-gradient(90deg, #87CEEB, #4682B4, #5F9EA0);
    width: 0%;
    transition: width 0.4s ease;
    box-shadow: 0 0 20px rgba(135, 206, 235, 0.8);
}

#statusText {
    font-weight: 600;
    margin-bottom: 1rem;
    color: #1e3a8a;
    font-size: 1.1rem;
}

#videoList {
    max-height: 300px;
    overflow-y: auto;
    margin-top: 1.5rem;
    padding: 1rem;
    background: rgba(255,255,255,0.5);
    border-radius: 15px;
    box-shadow: inset 2px 2px 4px rgba(0,0,0,0.1);
}

.video-item {
    padding: 1rem;
    margin-bottom: 0.8rem;
    border-radius: 12px;
    background: rgba(255,255,255,0.7);
    box-shadow: 
        4px 4px 8px rgba(135, 206, 235, 0.3),
        -2px -2px 4px rgba(255,255,255,0.9);
    transition: all 0.2s;
}

.video-item:hover {
    transform: translateX(5px);
    box-shadow: 6px 6px 12px rgba(135, 206, 235, 0.4);
}

.error {
    background: rgba(255, 182, 193, 0.4);
    color: #c53030;
    padding: 1.5rem;
    border-radius: 15px;
    margin-top: 1.5rem;
    border-left: 5px solid #ff6b6b;
    box-shadow: 0 4px 12px rgba(255, 107, 107, 0.3);
}

@media (max-width: 768px) {
    .input-group, #formatGroup {
        flex-direction: column;
    }
    
    #playlistUrl {
        min-width: auto;
    }
    
    .container {
        padding: 2rem 1.5rem;
        margin: 1rem;
    }
    
    h1 {
        font-size: 1.8rem;
    }
}

